name: cognitive_training_games
description: A collection of cognitive training games focusing on executive functions.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  cupertino_icons: ^1.0.2
  supabase_flutter: ^1.10.25
  http: ^1.1.0
  uuid: ^4.2.1
  fl_chart: ^0.65.0
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
